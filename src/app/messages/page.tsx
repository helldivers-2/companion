import Container from "@/components/containerCard";
import MessagesList from "@/components/currentMessages";

export default function MessagesPage() {
  return (
    <Container title="Major Orders">
      <MessagesList />
    </Container>
  );
}
